spring:
  datasource:
    url: jdbc:h2:mem:inventory_test;MODE=MySQL;DB_CLOSE_DELAY=-1;NON_KEYWORDS=USER
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

jwt:
  secret: test-secret-key-with-at-least-32-characters
  expiration: 3600000
